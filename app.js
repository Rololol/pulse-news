const topics=["Alle","Politik","Wissenschaft","Technologie","Gesundheit","Wirtschaft","Klima","Sport","Kultur","Raumfahrt","Energie","Digitales","Mobilität","Finanzen"];let data=[],topic="Alle";const nav=document.querySelector("#nav"),chips=document.querySelector("#topics"),news=document.querySelector("#news"),hero=document.querySelector("#hero"),search=document.querySelector("#search"),count=document.querySelector("#count");
nav.innerHTML=topics.slice(0,9).map(x=>'<button>'+x+'</button>').join("");chips.innerHTML='<div class="chips">'+topics.map(x=>'<button class="chip '+(x==="Alle"?"active":"")+'">'+x+'</button>').join("")+'</div>';
function render(){let q=search.value.toLowerCase();let list=data.filter(x=>(topic==="Alle"||x.topic===topic)&&(!q||(x.title+" "+x.summary+" "+x.source).toLowerCase().includes(q)));count.textContent=list.length+" Meldungen";hero.innerHTML=list[0]?'<div class="hero"><span class="meta">'+list[0].source+" · "+new Date(list[0].date).toLocaleString("de-DE")+'</span><h2>'+esc(list[0].title)+'</h2><p>'+esc(list[0].summary||"Aktuelle Meldung aus mehreren Quellen.")+'</p></div>':"";news.innerHTML=list.length?list.map(x=>'<article class="card"><span class="meta">'+esc(x.topic)+" · "+esc(x.country||"International")+'</span><h3>'+esc(x.title)+'</h3><p>'+esc(x.summary||"Quelle öffnen für Details.")+'</p><div class="source">'+esc(x.source)+' · <a href="'+x.url+'" target="_blank" rel="noopener">Quelle</a></div></article>').join(""):'<div class="empty">Keine Meldungen für diese Auswahl.</div>'}
function esc(s=""){return s.replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]))}
async function load(){try{let r=await fetch("data/news.json?"+Date.now());data=await r.json()}catch(e){data=[]}render()}
document.querySelectorAll(".chip").forEach(b=>b.onclick=()=>{topic=b.textContent;document.querySelectorAll(".chip").forEach(x=>x.classList.toggle("active",x===b));render()});document.querySelectorAll("#nav button").forEach(b=>b.onclick=()=>{topic=b.textContent;document.querySelectorAll(".chip").forEach(x=>x.classList.toggle("active",x.textContent===topic));render()});search.oninput=render;document.querySelector("#theme").onclick=()=>document.body.classList.toggle("dark");document.querySelectorAll(".bottom button").forEach(b=>b.onclick=()=>{if(b.dataset.nav==="search")search.focus();else if(b.dataset.nav==="topics")document.querySelector(".toolbar").scrollIntoView();else{topic="Alle";render()}});
load();setInterval(load,300000);